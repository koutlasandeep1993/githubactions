name: github-variables
run-name: github variables

on:
  push:
    branches:
      - main
   
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [ "$sandeep" == "ssss" ]
          then
            echo "got secrets"
          fi  
        env:
          sandeep: ${{ secrets.SANDEEP_ENV_NAME }}

